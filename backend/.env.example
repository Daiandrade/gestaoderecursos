# =====================================
# EXEMPLO DE CONFIGURAÇÃO
# Copie este arquivo para .env e ajuste
# =====================================

NODE_ENV=production
PORT=5000

# Gere com: node -e "console.log(require('crypto').randomBytes(48).toString('base64'))"
JWT_SECRET=COLE_AQUI_UMA_CHAVE_LONGA_E_ALEATORIA_MIN_32_CHARS

DB_PATH=./database.sqlite
ALLOWED_ORIGINS=
